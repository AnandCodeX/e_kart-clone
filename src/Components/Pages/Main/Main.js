import React from "react";
import { NavLink } from "react-router-dom";

const Main = () => {
  return (
    <div>
      <NavLink to="/login">Login</NavLink>
      
    </div>
  );
};

export default Main;
